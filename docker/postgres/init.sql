-- Optional initialization for the production PostgreSQL database.
-- The official postgres image already creates POSTGRES_DB from the environment,
-- so this script is intentionally a no-op placeholder for future extensions
-- such as custom extensions, roles, or schemas.
SELECT 1;
